//2017-08-08-16.27
//1001

#include <bits/stdc++.h>

using namespace std;

const int maxn = 5e4 + 100;
int a[maxn], b[maxn], vis[maxn], ans[maxn];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int T;
    cin >> T;
    while (T--) {
        int n, m, q, x;
        cin >> n >> m >> q;
        for (int i = 0; i < n; i++) {
            cin >> x;
            a[x] = T;
        }
        for (int j = 0; j < m; j++) {
            cin >> x;
            b[x] = T;
        }
        while (q--) {
            cin >> x;
            int ans = 0;
            for (int i = 0; i <)
        }
    }
}
